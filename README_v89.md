# Paper Log v89

`물욕템 or 메모` 속성을 득템 메모 저장/읽기 속성으로 유지합니다.

## 변경
- 새 `득템` 속성 사용 안 함
- `BOSS_RECORD_MEMO_PROP` / 기본값 `물욕템 or 메모`에서 득템 메모를 읽고 씁니다.
- 보스명/가족명과 매칭되는 줄만 해당 보스에 표시합니다.
- 매칭 실패 시 전체 메모를 모든 보스에 뿌리던 fallback을 제거했습니다.

## 메모 권장 형식
하드 윌: 거공 5000000000
카오스 더스크: 칠흑 3000000000
