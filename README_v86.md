# Paper Log v86

득템/메모 불러오기 fallback 보완.

- 보스 기록 DB 메모에서 보스명/가족명 매칭 실패 시 빈 값으로 버리지 않고 전체 메모를 유지합니다.
- 콜론 없이 작성된 메모 예: `거공 5000000000`, `하드 윌 거공 5000000000` 도 불러올 수 있게 했습니다.
- 숫자 파싱도 공백 없이 붙은 숫자까지 잡도록 완화했습니다.

주의: 여러 보스의 득템 메모가 한 칸에 섞여 있고 보스명이 명확히 분리되지 않으면 일부 중복 집계될 수 있습니다.
